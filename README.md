# SSHRC

If you have `sshrc` installed, this package will alias `ssh` to `sshrc`.

## Installation

1. ```zsh
   #.zimrc
   zmodule bgrrtt/zimfw-sshrc -n sshrc
    ```

2. ```zsh
   zimfw install
   ```
